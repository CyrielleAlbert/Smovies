export default function Header({name,nStars,movies, ...props }) {

    return(
        <div style={{
            flexDirection: "row",
            flexWrap:"wrap",
            display:"flex"
        }}>
        </div>
    )
}
